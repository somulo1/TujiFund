import React from 'react';

const Achievements: React.FC = () => {
    return (
        <div>
            <h2>Achievements</h2>
            {/* Add functionality for achievements here */}
        </div>
    );
};

export default Achievements;
